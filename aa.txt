各cpp内で描写ではなく一つのCpp内ですべて描写すれば実装可能かも？
共通で使う配列を作っておき、各Cpp内のDrawにて共通で使う配列に書き込み、もし同じ座標に物体があれば上書き
最後に共通の配列を表示するようにする

また共通の配列はステージ全体分の高さと幅を用意しておき
Player.PositionY+-12

//80は幅の値
for(int i=0;i<80;i++)
{
      
      for(int j=Player.PositionY-12;j<Player.PositionY+12;j++)
    {
      gotoxy(i,j);
      //この中で共通の配列の中に文字または数字があるか、などを確認し描写
      
    }

  
}


注意点

描写の優先順位


画面全体を再描写するため画面の点灯//これは動いている物のみを再描写すれば対処可能

